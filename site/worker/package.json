{
  "name": "spikes-sh-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create spikes-sh-db",
    "db:migrate": "wrangler d1 execute spikes-sh-db --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.95.0"
  }
}
