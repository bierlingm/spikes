{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create {{DB_NAME}}",
    "db:migrate": "wrangler d1 execute {{DB_NAME}} --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute {{DB_NAME}} --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.0"
  }
}
