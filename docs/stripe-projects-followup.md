# Stripe projects.dev — follow-up email draft

Two weeks out from the original submission, no response. Template for a polite nudge. Short, gives them a new reason to say yes (the device-code auth ships, the $49 lifetime pricing is live, NPM downloads are climbing), easy to reply to.

---

**To:** (whatever contact you used — projects@stripe.dev, projects-dev@stripe.com, or the form)

**Subject:** Re: Spikes — projects.dev partner submission (quick update)

Hi (name if you have it, otherwise "Stripe projects team"),

I submitted Spikes (https://spikes.sh) for a projects.dev listing about two weeks ago and wanted to share a few updates in case any of them move the needle:

- **Lifetime pricing is live** — $49 one-time Pro tier, powered by Stripe Checkout. No subscription churn, which keeps the economics simple for dev-tool buyers.
- **Device-code browser auth ships this week** (RFC 8628-style, same pattern Stripe CLI uses for `stripe login`). The CLI → dashboard → CLI round-trip is roughly 4 seconds end to end.
- **`spikes-mcp` is published on NPM** and about to be listed on the official MCP Registry — which puts Spikes in front of the Claude/Cursor/Windsurf users projects.dev tends to index.

Happy to send a 30-second demo, or cut anything from the original submission that no longer fits your current bar. Whatever's most useful.

Thanks,
Moritz
[hi@spikes.sh] · https://spikes.sh

---

## Notes before sending

- Swap "this week" for "just shipped" once device code auth is published publicly.
- If you have the original thread, reply in it rather than starting a new one — it keeps context.
- If no response after another 10 days, switch venues: DM on Twitter/X to the projects.dev account or a Stripe dev advocate you follow (Patrick Collison, David Hoang, etc.). Never cold-DM from zero relationship — only if they've already engaged with something spikes-adjacent.
